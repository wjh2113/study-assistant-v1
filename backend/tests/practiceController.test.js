/** * Practice Controller API Tests * 测试练习会话管理功能 *  * 覆盖场景�? * - 创建练习会话 * - 获取会话列表 * - 获取会话详情 * - 更新会话状�? * - 删除会话 * - 添加问题到会�? * - 提交答案 * - 获取答题记录 * - 边界测试和安全测�? */const request = require('supertest');const app = require('../src/server');const {   AuthHelper,   DbCleaner,  generatePhone,  generateId,  assertions,  createPracticeSession,  createQuestion,  createAnswerRecord} = require('./test-utils');describe('Practice Controller API Tests', () => {  let server;  let authHelper;  let dbCleaner;  let db;  let authToken;  let testUser;  // ============================================================================  // 测试生命周期  // ============================================================================  beforeAll(async () => {    console.log('🧪 开�?Practice Controller 模块测试');        server = app.listen(0);    await new Promise(resolve => server.on('listening', resolve));    console.log(`🔌 测试服务器启动在端口 ${server.address().port}`);        authHelper = new AuthHelper(server);        try {      db = require('../src/database');      dbCleaner = new DbCleaner(db);    } catch (error) {      console.warn('⚠️  数据库清理不可用');    }  }, 30000);  afterAll(async () => {    console.log('�?Practice Controller 模块测试完成');        if (dbCleaner) {      await dbCleaner.cleanupAll();    }        if (server) {      server.close();    }  });  beforeEach(async () => {    if (dbCleaner) {      await dbCleaner.cleanupAll();    }        const loginResult = await authHelper.createAndLogin();    authToken = loginResult.token;    testUser = loginResult.user;  });  afterEach(async () => {    if (dbCleaner && testUser) {      await dbCleaner.cleanupUser(testUser.id);    }    authHelper.cleanup();  });  // ============================================================================  // POST /api/practice/sessions - 创建练习会话  // ============================================================================  describe('POST /api/practice/sessions - 创建练习会话', () => {    it('应该成功创建练习会话', async () => {      const sessionData = {        textbookId: generateId(),        unitId: 'unit_1'      };      const res = await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send(sessionData);      assertions.assertSuccessResponse(res, 201);      assertions.assertHasFields(res.body, ['message', 'data']);      assertions.assertValidPracticeSession(res.body.data);      expect(res.body.data.status).toBe('active');    });    it('应该拒绝未授权请�?, async () => {      const sessionData = { textbookId: generateId(), unitId: 'unit_1' };      const res = await request(server)        .post('/api/practice/sessions')        .send(sessionData);      assertions.assertErrorResponse(res, 401);    });    it('应该拒绝缺少必需字段', async () => {      const res = await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({});      assertions.assertErrorResponse(res, 400);    });    it('应该拒绝无效�?unitId 格式', async () => {      const res = await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({ textbookId: generateId(), unitId: '' });      expect(res.statusCode).toBe(400);    });  });  // ============================================================================  // GET /api/practice/sessions - 获取会话列表  // ============================================================================  describe('GET /api/practice/sessions - 获取会话列表', () => {    beforeEach(async () => {      // 创建测试会话      await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({ textbookId: generateId(), unitId: 'unit_1' });            await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({ textbookId: generateId(), unitId: 'unit_2' });    });    it('应该成功获取会话列表', async () => {      const res = await request(server)        .get('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`);      assertions.assertSuccessResponse(res);      assertions.assertHasFields(res.body, ['data', 'total']);      expect(Array.isArray(res.body.data)).toBe(true);      expect(res.body.total).toBeGreaterThanOrEqual(2);    });    it('应该支持状态过�?, async () => {      const res = await request(server)        .get('/api/practice/sessions?status=active')        .set('Authorization', `Bearer ${authToken}`);      assertions.assertSuccessResponse(res);      res.body.data.forEach(session => {        expect(session.status).toBe('active');      });    });    it('应该支持分页', async () => {      const res = await request(server)        .get('/api/practice/sessions?limit=1&offset=0')        .set('Authorization', `Bearer ${authToken}`);      assertions.assertSuccessResponse(res);      expect(res.body.data.length).toBeLessThanOrEqual(1);    });    it('应该只返回当前用户的会话', async () => {      // 创建另一个用�?      const anotherUser = await authHelper.createAndLogin({        phone: generatePhone()      });      await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${anotherUser.token}`)        .send({ textbookId: generateId(), unitId: 'unit_3' });      const res = await request(server)        .get('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`);      assertions.assertSuccessResponse(res);      res.body.data.forEach(session => {        expect(session.user_id).toBe(testUser.id);      });    });  });  // ============================================================================  // GET /api/practice/sessions/:id - 获取会话详情  // ============================================================================  describe('GET /api/practice/sessions/:id - 获取会话详情', () => {    let sessionId;    beforeEach(async () => {      const createRes = await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({ textbookId: generateId(), unitId: 'unit_1' });            sessionId = createRes.body.data.id;    });    it('应该成功获取会话详情', async () => {      const res = await request(server)        .get(`/api/practice/sessions/${sessionId}`)        .set('Authorization', `Bearer ${authToken}`);      assertions.assertSuccessResponse(res);      assertions.assertHasFields(res.body.data, ['id', 'user_id', 'textbook_id', 'status']);      expect(res.body.data.id).toBe(sessionId);    });    it('应该拒绝访问不存在的会话', async () => {      const fakeId = generateId();            const res = await request(server)        .get(`/api/practice/sessions/${fakeId}`)        .set('Authorization', `Bearer ${authToken}`);      assertions.assertErrorResponse(res, 404);    });    it('应该拒绝访问其他用户的会�?, async () => {      const anotherUser = await authHelper.createAndLogin({        phone: generatePhone()      });      const res = await request(server)        .get(`/api/practice/sessions/${sessionId}`)        .set('Authorization', `Bearer ${anotherUser.token}`);      assertions.assertErrorResponse(res, 404);    });    it('应该拒绝无效�?ID 格式', async () => {      const res = await request(server)        .get('/api/practice/sessions/invalid-id')        .set('Authorization', `Bearer ${authToken}`);      expect(res.statusCode).toBe(404);    });  });  // ============================================================================  // PUT /api/practice/sessions/:id - 更新会话  // ============================================================================  describe('PUT /api/practice/sessions/:id - 更新会话', () => {    let sessionId;    beforeEach(async () => {      const createRes = await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({ textbookId: generateId(), unitId: 'unit_1' });            sessionId = createRes.body.data.id;    });    it('应该成功更新会话状�?, async () => {      const updateData = { status: 'completed', score: 90 };      const res = await request(server)        .put(`/api/practice/sessions/${sessionId}`)        .set('Authorization', `Bearer ${authToken}`)        .send(updateData);      assertions.assertSuccessResponse(res);      expect(res.body.data.status).toBe('completed');      expect(res.body.data.score).toBe(90);    });    it('应该拒绝更新不存在的会话', async () => {      const fakeId = generateId();            const res = await request(server)        .put(`/api/practice/sessions/${fakeId}`)        .set('Authorization', `Bearer ${authToken}`)        .send({ status: 'completed' });      assertions.assertErrorResponse(res, 404);    });    it('应该拒绝更新其他用户的会�?, async () => {      const anotherUser = await authHelper.createAndLogin({        phone: generatePhone()      });      const res = await request(server)        .put(`/api/practice/sessions/${sessionId}`)        .set('Authorization', `Bearer ${anotherUser.token}`)        .send({ status: 'completed' });      assertions.assertErrorResponse(res, 404);    });    it('应该拒绝无效的状态�?, async () => {      const res = await request(server)        .put(`/api/practice/sessions/${sessionId}`)        .set('Authorization', `Bearer ${authToken}`)        .send({ status: 'invalid_status' });      expect(res.statusCode).toBe(400);    });  });  // ============================================================================  // DELETE /api/practice/sessions/:id - 删除会话  // ============================================================================  describe('DELETE /api/practice/sessions/:id - 删除会话', () => {    let sessionId;    beforeEach(async () => {      const createRes = await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({ textbookId: generateId(), unitId: 'unit_1' });            sessionId = createRes.body.data.id;    });    it('应该成功删除会话', async () => {      const res = await request(server)        .delete(`/api/practice/sessions/${sessionId}`)        .set('Authorization', `Bearer ${authToken}`);      assertions.assertSuccessResponse(res);      expect(res.body).toHaveProperty('message');    });    it('应该拒绝删除不存在的会话', async () => {      const fakeId = generateId();            const res = await request(server)        .delete(`/api/practice/sessions/${fakeId}`)        .set('Authorization', `Bearer ${authToken}`);      assertions.assertErrorResponse(res, 404);    });    it('应该拒绝删除其他用户的会�?, async () => {      const anotherUser = await authHelper.createAndLogin({        phone: generatePhone()      });      const res = await request(server)        .delete(`/api/practice/sessions/${sessionId}`)        .set('Authorization', `Bearer ${anotherUser.token}`);      assertions.assertErrorResponse(res, 404);    });  });  // ============================================================================  // POST /api/practice/sessions/:id/questions - 添加问题  // ============================================================================  describe('POST /api/practice/sessions/:id/questions - 添加问题', () => {    let sessionId;    beforeEach(async () => {      const createRes = await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({ textbookId: generateId(), unitId: 'unit_1' });            sessionId = createRes.body.data.id;    });    it('应该成功添加问题', async () => {      const questionData = {        type: 'multiple_choice',        question: '1 + 1 = ?',        options: ['A. 1', 'B. 2', 'C. 3', 'D. 4'],        answer: 'B',        explanation: '1 �?1 等于 2',        order: 1      };      const res = await request(server)        .post(`/api/practice/sessions/${sessionId}/questions`)        .set('Authorization', `Bearer ${authToken}`)        .send(questionData);      assertions.assertSuccessResponse(res, 201);      assertions.assertHasFields(res.body.data, ['id', 'session_id', 'question', 'answer']);    });    it('应该拒绝未授权请�?, async () => {      const questionData = { type: 'multiple_choice', question: '测试', answer: 'A' };      const res = await request(server)        .post(`/api/practice/sessions/${sessionId}/questions`)        .send(questionData);      assertions.assertErrorResponse(res, 401);    });    it('应该拒绝缺少必需字段', async () => {      const res = await request(server)        .post(`/api/practice/sessions/${sessionId}/questions`)        .set('Authorization', `Bearer ${authToken}`)        .send({});      assertions.assertErrorResponse(res, 400);    });    it('应该拒绝访问不存在的会话', async () => {      const fakeId = generateId();            const res = await request(server)        .post(`/api/practice/sessions/${fakeId}/questions`)        .set('Authorization', `Bearer ${authToken}`)        .send({ type: 'multiple_choice', question: '测试', answer: 'A' });      assertions.assertErrorResponse(res, 404);    });  });  // ============================================================================  // POST /api/practice/sessions/:id/answers - 提交答案  // ============================================================================  describe('POST /api/practice/sessions/:id/answers - 提交答案', () => {    let sessionId;    let questionId;    beforeEach(async () => {      const sessionRes = await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({ textbookId: generateId(), unitId: 'unit_1' });            sessionId = sessionRes.body.data.id;      const questionRes = await request(server)        .post(`/api/practice/sessions/${sessionId}/questions`)        .set('Authorization', `Bearer ${authToken}`)        .send({          type: 'multiple_choice',          question: '测试题目',          options: ['A. 1', 'B. 2'],          answer: 'A',          order: 1        });            questionId = questionRes.body.data.id;    });    it('应该成功提交答案', async () => {      const answerData = {        questionId,        answer: 'A',        isCorrect: true      };      const res = await request(server)        .post(`/api/practice/sessions/${sessionId}/answers`)        .set('Authorization', `Bearer ${authToken}`)        .send(answerData);      assertions.assertSuccessResponse(res, 201);      assertions.assertHasFields(res.body.data, ['id', 'user_id', 'question_id', 'answer']);    });    it('应该拒绝未授权请�?, async () => {      const res = await request(server)        .post(`/api/practice/sessions/${sessionId}/answers`)        .send({ questionId, answer: 'A' });      assertions.assertErrorResponse(res, 401);    });    it('应该拒绝缺少必需字段', async () => {      const res = await request(server)        .post(`/api/practice/sessions/${sessionId}/answers`)        .set('Authorization', `Bearer ${authToken}`)        .send({});      assertions.assertErrorResponse(res, 400);    });  });  // ============================================================================  // GET /api/practice/sessions/:id/answers - 获取答题记录  // ============================================================================  describe('GET /api/practice/sessions/:id/answers - 获取答题记录', () => {    let sessionId;    beforeEach(async () => {      const sessionRes = await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({ textbookId: generateId(), unitId: 'unit_1' });            sessionId = sessionRes.body.data.id;      // 添加问题并提交答�?      const questionRes = await request(server)        .post(`/api/practice/sessions/${sessionId}/questions`)        .set('Authorization', `Bearer ${authToken}`)        .send({          type: 'multiple_choice',          question: '测试题目',          options: ['A. 1', 'B. 2'],          answer: 'A',          order: 1        });      await request(server)        .post(`/api/practice/sessions/${sessionId}/answers`)        .set('Authorization', `Bearer ${authToken}`)        .send({          questionId: questionRes.body.data.id,          answer: 'A',          isCorrect: true        });    });    it('应该成功获取答题记录', async () => {      const res = await request(server)        .get(`/api/practice/sessions/${sessionId}/answers`)        .set('Authorization', `Bearer ${authToken}`);      assertions.assertSuccessResponse(res);      assertions.assertHasFields(res.body, ['data', 'total']);      expect(Array.isArray(res.body.data)).toBe(true);      expect(res.body.total).toBeGreaterThanOrEqual(1);    });    it('应该拒绝访问不存在的会话', async () => {      const fakeId = generateId();            const res = await request(server)        .get(`/api/practice/sessions/${fakeId}/answers`)        .set('Authorization', `Bearer ${authToken}`);      assertions.assertErrorResponse(res, 404);    });    it('应该拒绝访问其他用户的会�?, async () => {      const anotherUser = await authHelper.createAndLogin({        phone: generatePhone()      });      const res = await request(server)        .get(`/api/practice/sessions/${sessionId}/answers`)        .set('Authorization', `Bearer ${anotherUser.token}`);      assertions.assertErrorResponse(res, 404);    });  });  // ============================================================================  // 边界测试场景  // ============================================================================  describe('边界测试', () => {    it('应该处理大量问题', async () => {      const sessionRes = await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({ textbookId: generateId(), unitId: 'unit_1' });            const sessionId = sessionRes.body.data.id;      // 添加 50 个问�?      for (let i = 0; i < 50; i++) {        const res = await request(server)          .post(`/api/practice/sessions/${sessionId}/questions`)          .set('Authorization', `Bearer ${authToken}`)          .send({            type: 'multiple_choice',            question: `测试题目 ${i}`,            options: ['A. 1', 'B. 2'],            answer: 'A',            order: i          });                expect(res.statusCode).toBe(201);      }    });    it('应该处理超长问题文本', async () => {      const sessionRes = await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({ textbookId: generateId(), unitId: 'unit_1' });            const sessionId = sessionRes.body.data.id;      const longQuestion = 'a'.repeat(5000);      const res = await request(server)        .post(`/api/practice/sessions/${sessionId}/questions`)        .set('Authorization', `Bearer ${authToken}`)        .send({          type: 'multiple_choice',          question: longQuestion,          options: ['A. 1', 'B. 2'],          answer: 'A',          order: 1        });      expect([201, 400]).toContain(res.statusCode);    });    it('应该处理并发请求', async () => {      const sessionRes = await request(server)        .post('/api/practice/sessions')        .set('Authorization', `Bearer ${authToken}`)        .send({ textbookId: generateId(), unitId: 'unit_1' });            const sessionId = sessionRes.body.data.id;      const promises = Array(5).fill(null).map((_, i) =>        request(server)          .post(`/api/practice/sessions/${sessionId}/questions`)          .set('Authorization', `Bearer ${authToken}`)          .send({            type: 'multiple_choice',            question: `并发测试 ${i}`,            options: ['A. 1', 'B. 2'],            answer: 'A',            order: i          })      );      const results = await Promise.all(promises);      results.forEach(res => {        expect([201, 400, 429]).toContain(res.statusCode);      });    });  });});